[functions.chat-stream]
# Gateway verify_jwt disabled: incompatible with new sb_publishable_ API key format.
# JWT verification is handled in _shared/auth.ts via supabase.auth.getUser(jwt).
verify_jwt = false
